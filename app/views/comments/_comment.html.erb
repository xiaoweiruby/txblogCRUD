<%= comment.name %>
<%= comment.body %>
<%= time_ago_in_words(comment.created_at) %>

<%= link_to 'Delete', [comment.post, comment],
                                  method: :delete,
                                  class: "button",
                                    data: { confirm: 'Are you sure?' } %></p>
